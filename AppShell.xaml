<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WeatherApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherApp"
    Shell.FlyoutBehavior="Flyout"
    Title="WeatherApp">

    <ShellContent
        
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    
    <ShellContent Title="Заметки"
              ContentTemplate="{DataTemplate local:NotesPage}"
              Route="NotesPage" />
    
    <ShellContent Route="notespage" ContentTemplate="{DataTemplate views:NotesPage}" />

    <ShellContent Title="Календарь" ContentTemplate="{DataTemplate local:CalendarPage}" />

</Shell>
